name: DDInter Scraper Test Run

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4

      - name: Run Scraper (Exhaustive V5 Test 5 Drugs)
        run: |
          cd assets/external_research_data
          python bulk_scraper_v5.py

      - name: Upload Results as Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ddinter-exhaustive-v5-data
          path: |
            assets/external_research_data/ddinter_exhaustive_v5.json
            assets/external_research_data/ddinter_drugs_metadata_v5.csv
            assets/external_research_data/ddinter_interactions_v5.csv
