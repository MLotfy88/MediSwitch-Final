# تقرير تحليل الفجوة: TTD-IDRBLab vs. MediSwitch Dosage Table

**التاريخ:** 2026-01-09  
**المقارنة:** جدول `dosage_guidelines` المحلي مقابل `TTD_Complete_Merged.csv` (بيانات TTD المجمعة)

---

## 1. 🔍 نظرة عامة
- **جدول الجرعات المحلي (`dosage_guidelines`)**: مصمم بدقة عالية لحفظ أرقام الجرعات، التردد، وتعديلات الجرعات (30 عموداً).
- **بيانات TTD (`IDRBLab`)**: تركز على هوية الدواء، الشركة المصنعة، الحالة القانونية، وعلاقاته بالأمراض والمستهدفات البيولوجية.

⚠️ **النتيجة الرئيسية:** لا يوجد تقاطع مباشر في أعمدة "الجرعات الرقمية". TDD ليس مصدراً للجرعة (Dose)، بل هو مصدر لـ "هوية الدواء وسياقه العلاجي".

---

## 2. 🚫 الأعمدة المفقودة تماماً في TTD
البيانات التالية موجودة في هيكل قاعدة بياناتك **ولكن لا يوجد لها أي مقابل** في ملفات TTD الحالية:

| العمود المفقود (Local DB) | الأهمية | هل يمكن استخراجه بـ Parsing؟ |
|---------------------------|---------|------------------------------|
| `min_dose` / `max_dose` | 🔴 حرجة | ❌ لا (لا توجد نصوص جرعات في الملفات) |
| `frequency` | 🔴 حرجة | ❌ لا |
| `duration` | 🟠 متوسطة | ❌ لا |
| `dose_unit` | 🔴 حرجة | ❌ لا |
| `route` | 🔴 حرجة | ❌ لا (غير موجود في الملفات المتاحة) |
| `renal_adjustment` | 🟡 مهمة | ❌ لا |
| `hepatic_adjustment` | 🟡 مهمة | ❌ لا |
| `pregnancy_category` | 🟡 مهمة | ❌ لا (موجود فقط كـ warnings عامة في مصادر أخرى) |
| `contraindications` | 🟡 مهمة | ❌ لا (إلا إذا حملنا ملفات Drug Combinations) |
| `adverse_reactions` | 🟡 مهمة | ❌ لا |

> **الخلاصة:** لا يمكن استخدام TTD لملء جدول الجرعات الحالي مباشرة.

---

## 3. 🛠️ الأعمدة القابلة للاستخراج (Parsing / Mapping)
يمكننا استخدام بيانات TTD لملء أو تحسين بعض الحقول في قاعدة البيانات، ولكن بطرق غير مباشرة:

| العمود في TTD | العمود المقابل محلياً | طريقة الاستفادة (Parsing Logic) |
|---------------|------------------------|---------------------------------|
| `Primary_Disease` | `condition` | ✅ **ممتاز!** يمكن استبدال وصف الحالة المرضية باسم المرض القياسي ورقم ICD-11. <br> *مثال:* `Hypertension` (ICD: BA00) بدلاً من `High blood pressure`. |
| `Primary_ICD_Code` | *(جديد مقترح)* | ✅ إضافة عمود `icd_code` لجدول الجرعات لربط الجرعة بكود مرض عالمي. |
| `Approval_Status` | `warnings` | ⚠️ يمكن إضافة تحذير إذا كانت الحالة `Withdrawn` أو `Investigational`. |
| `Synonyms` | `drugs.trade_name` | 🔍 تحسين البحث: لا يملأ جدول الجرعات، لكن يربط المستخدم بالدواء الصحيح. |

---

## 4. 💎 بيانات إضافية في TTD (إثراء المحتوى)
هذه البيانات **موجودة في TTD وغير موجودة لديك**، وتمثل فرصة ذهبية لإضافة ميزات جديدة للتطبيق:

### أ. الهوية والتصنيف (لصفحة تفاصيل الدواء)
- **`Therapeutic_Class`**: (مثل `Anticancer Agents`, `Antiviral`). مفيد للتصنيف والفلترة.
- **`Company`**: الشركة المصنعة الأصلية.
- **`Drug_Type`**: (`Small molecule`, `Biotech`).

### ب. السياق الطبي العميق (للمحترفين)
- **`Primary_MOA` (Mechanism of Action)**: آلية عمل الدواء (مثل `Inhibitor`, `Agonist`). معلومة قيمة للأطباء والصيادلة.
- **`Primary_Target`**: المستهدف البيولوجي (اسم البروتين أو المستقبل).
- **`InChIKey` / `SMILES`**: الهيكل الكيميائي (مفيد فقط للبحث المتقدم أو عرض الهيكل).

---

## 5. 💡 التوصيات (The Action Plan)

1. **لا تعتمد على TTD للجرعات:** استمر في استخدام DailyMed و WHO كمصادر للجرعات (`min/max dose`, `frequency`).
2. **استخدم TTD لـ "تنظيف الأمراض":**
   - أنشئ جدولاً جديداً `disease_codes` يحتوي على `Disease Name` و `ICD Code` من TTD.
   - اربط الجرعات بهذا الجدول بدلاً من كتابة اسم المرض يدوياً في حقل `condition`.
3. **أضف ميزة "معلومات الدواء" (Drug Info):**
   - أضف أعمدة لجدول `drugs`: `therapeutic_class`, `mechanism_of_action`, `approval_status`.
   - املأها من TTD لإعطاء المستخدم "بطاقة تعريف" للدواء.
4. **انتظر ملفات التفاعلات:**
   - البيانات المفقودة حول `contraindications` ستأتي من ملفات "Drug Combinations" التي طلبتها، وليس من الملفات الحالية.

---

### 📊 الخلاصة البصرية
- **الجرعات (Dosage Logic):** ⬜️ TTD فارغ تماماً.
- **الأمراض (Disease Context):** 🟩 TTD قوي جداً (ICD-11 codes).
- **البيانات الوصفية (Metadata):** 🟩 TTD ممتاز (Class, MOA, Company).
