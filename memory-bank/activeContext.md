# السياق النشط - MediSwitch (Active Context)

## 🎯 التركيز الحالي (Current Focus)
ينصب تركيز فريق التطوير حاليًا بشكل كامل على **إطلاق الإصدار الأول (MVP 1.0)**. الهدف الأسمى هو ضمان استقرار التطبيق، إتمام عملية الربط مع الـ Backend المنشور، وتجهيز حزمة التطبيق للنشر النهائي على المتاجر.

## 🔄 التغييرات الأخيرة (Recent Changes)

### الواجهة الخلفية (Backend)
-   ✅ **فصل الكود**: تم فصل كود الـ Backend في مستودع مستقل لتسهيل الإدارة والنشر.
-   ✅ **النشر (Deployment)**: تم نشر الـ Backend بنجاح على خادم VPS (IP: 37.27.185.59).
-   ✅ **البنية التحتية**: إعداد بيئة Docker كاملة مع Nginx و PostgreSQL.
-   ✅ **تطوير API**: إنشاء وتفعيل نقاط النهاية (Endpoints) الخاصة بالإعدادات العامة، تحميل البيانات، وتسجيل التحليلات.

### الواجهة الأمامية (Frontend)
-   ✅ **تحسين الواجهة**: تحديث شامل لواجهة المستخدم لتتطابق مع معايير التصميم الجديد (Design Lab).
-   ✅ **إصلاحات الأداء**: حل مشاكل تحميل البيانات في الشاشة الرئيسية لضمان عرض سلس.
-   ✅ **إعادة الهيكلة**: تحسين هيكل المشروع ليتوافق تمامًا مع Clean Architecture.
-   ✅ **الإعلانات**: دمج مكتبة AdMob وإعداد الوحدات الإعلانية (باستخدام معرفات الاختبار).
-   ✅ **الإعدادات**: بناء شاشات الإعدادات والاشتراكات وتجهيزها وظيفيًا.

## 📝 الخطوات التالية (Next Steps)

1.  **الربط والتكامل (Integration)**:
    -   تحديث `RemoteDataSource` في التطبيق ليتواصل مع الـ API الجديد المنشور (`http://37.27.185.59/api/`).
    -   تفعيل آلية مزامنة البيانات لتنزيل وتحديث ملف CSV تلقائيًا عند توفر إصدار جديد.
    -   جلب إعدادات الإعلانات والتطبيق ديناميكيًا من الـ Backend.

2.  **ضمان الجودة والاختبار (Testing & QA)**:
    -   إجراء اختبارات يدوية شاملة لجميع سيناريوهات الاستخدام (Happy & Edge Cases).
    -   التحقق الصارم من عمل التطبيق في وضعي "متصل" و "غير متصل" (Offline/Online).
    -   اختبار عملية تحديث قاعدة البيانات من البداية للنهاية.

3.  **تجهيز الإصدار (Release Preparation)**:
    -   استبدال معرفات إعلانات الاختبار (Test IDs) بمعرفات الإنتاج الحقيقية (Production IDs).
    -   إنشاء وتأمين مفاتيح التوقيع (Signing Keys) الخاصة بـ Android.
    -   بناء النسخة النهائية للتطبيق (APK و AAB) وتجهيزها للرفع.

## 💡 القرارات النشطة (Active Decisions)

-   **تأجيل الميزات**: تم اتخاذ قرار استراتيجي بتأجيل تفعيل واجهة "حاسبة الجرعات" و "مدقق التفاعلات" إلى الإصدارات اللاحقة (v1.1, v1.2) للتركيز على جودة واستقرار وظائف البحث والبيانات الأساسية في الـ MVP.
-   **استراتيجية البيانات**: الاستمرار في استخدام SQLite محليًا وعدم تطبيق التشفير في الوقت الحالي لضمان أفضل أداء ممكن على جميع الأجهزة، مع إعادة تقييم الحاجة للتشفير مستقبلاً.
-   **نموذج الربح**: الاعتماد على AdMob كمصدر دخل أساسي في مرحلة الإطلاق، مع تقديم خيار إزالة الإعلانات لاحقًا.

## 📌 ملاحظات للمطور (Developer Notes)
-   يجب تحديث ملف `mediswitch_plan.md` فور الانتهاء من أي مهمة لضمان دقة التتبع.
-   راجع ملف `errors.txt` في حال واجهت أي مشاكل غير متوقعة أثناء التطوير.
-   الـ Backend يعمل داخل حاويات Docker على الـ VPS؛ تأكد من تشغيل الحاوية المناسبة قبل محاولة إجراء أي تعديلات على الخادم.
