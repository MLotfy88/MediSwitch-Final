name: Daily Drug Price Update

on:
  schedule:
    # Run ~24 times per day with human-like random intervals
    # Times are spread irregularly to mimic human behavior
    - cron: '7 0 * * *'   # ~12:07 AM UTC
    - cron: '23 1 * * *'  # ~1:23 AM UTC
    - cron: '41 2 * * *'  # ~2:41 AM UTC
    - cron: '14 3 * * *'  # ~3:14 AM UTC
    - cron: '52 4 * * *'  # ~4:52 AM UTC
    - cron: '19 5 * * *'  # ~5:19 AM UTC
    - cron: '38 6 * * *'  # ~6:38 AM UTC
    - cron: '5 7 * * *'   # ~7:05 AM UTC
    - cron: '47 8 * * *'  # ~8:47 AM UTC
    - cron: '16 9 * * *'  # ~9:16 AM UTC
    - cron: '34 10 * * *' # ~10:34 AM UTC
    - cron: '9 11 * * *'  # ~11:09 AM UTC
    - cron: '51 12 * * *' # ~12:51 PM UTC
    - cron: '28 13 * * *' # ~1:28 PM UTC
    - cron: '3 14 * * *'  # ~2:03 PM UTC
    - cron: '45 15 * * *' # ~3:45 PM UTC
    - cron: '12 16 * * *' # ~4:12 PM UTC
    - cron: '56 17 * * *' # ~5:56 PM UTC
    - cron: '21 18 * * *' # ~6:21 PM UTC
    - cron: '39 19 * * *' # ~7:39 PM UTC
    - cron: '8 20 * * *'  # ~8:08 PM UTC
    - cron: '50 21 * * *' # ~9:50 PM UTC
    - cron: '27 22 * * *' # ~10:27 PM UTC
    - cron: '44 23 * * *' # ~11:44 PM UTC
  workflow_dispatch:  # Allow manual trigger

jobs:
  scrape-and-update:
    runs-on: ubuntu-latest
    
    steps:
      # إرسال إشعار ببدء الـ Workflow
      - name: Send Slack Notification - Start
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_USERNAME: MediSwitch Bot
          SLACK_ICON_EMOJI: ':pill:'
          SLACK_COLOR: '#007ec6'
          SLACK_MESSAGE: '🔄 بدء تحديث قاعدة بيانات الأدوية...\n*المستودع:* `${{ github.repository }}`\n*الوقت:* $(date -u +"%Y-%m-%d %H:%M:%S UTC")'
      
      - name: Random delay (human-like behavior)
        run: |
          # Add random delay between 0-300 seconds (0-5 minutes)
          DELAY=$((RANDOM % 300))
          echo "Waiting ${DELAY} seconds to mimic human behavior..."
          sleep $DELAY
      
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 pandas openpyxl
      
      - name: Run scraper
        run: |
          python3 scraper.py
      
      - name: Enrich data
        run: |
          python3 enrich_data.py meds_updated.csv meds_enriched.csv
      
      - name: Convert to JSON
        run: |
          python3 csv_to_json.py meds_enriched.csv drugs.json
      
      - name: Update app database
        run: |
          # Update the main app database file
          mkdir -p assets
          cp meds_enriched.csv assets/meds.csv
          echo "✅ Updated assets/meds.csv with $(wc -l <assets/meds.csv) lines"
      
      - name: Upload to Cloudflare Worker
        env:
          WORKER_URL: ${{ secrets.WORKER_URL }}
          API_KEY: ${{ secrets.WORKER_API_KEY }}
        run: |
          # Upload to Worker
          curl -X POST "$WORKER_URL/api/update" \
            -H "Authorization: Bearer $API_KEY" \
            -H "Content-Type: application/json" \
            -d @drugs.json \
            -o response.json
          
          cat response.json
          
          # Check success
          if grep -q "\"success\":true" response.json; then
            echo "✓ Upload successful to Cloudflare Worker"
          else
            echo "✗ Upload failed"
            cat response.json
            exit 1
          fi
      
      - name: Commit updated database
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # Check if there are changes
          if git diff --quiet assets/meds.csv; then
            echo "No changes in assets/meds.csv"
          else
            git add assets/meds.csv
            git commit -m "Auto-update: Daily drug database sync - $(date +'%Y-%m-%d')"
            git push
            echo "✅ Pushed updated database to repository"
          fi
      
      - name: Create summary and extract recent changes
        id: summary
        if: always()
        run: |
          echo "## Daily Drug Price Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Run Time:** $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
          
          if [ -f response.json ]; then
            echo "- **Status:** ✅ Upload completed" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
            cat response.json >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Status:** ❌ Upload failed" >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ -f assets/meds.csv ]; then
            DRUG_COUNT=$(wc -l <assets/meds.csv)
            echo "- **Database:** $DRUG_COUNT drugs in assets/meds.csv" >> $GITHUB_STEP_SUMMARY
            echo "total_drugs=$DRUG_COUNT" >> $GITHUB_OUTPUT
            
            # Extract top 5 recently updated drugs for Slack notification
            # CSV columns: trade_name(1), arabic_name(2), price(3), old_price(4), ..., last_price_update(10)
            DRUGS_INFO=""
            tail -n 6 assets/meds.csv | head -n 5 | while IFS=',' read -r trade_name arabic_name price old_price active company description dosage_form concentration last_update rest; do
              # Remove quotes
              trade_name=$(echo "$trade_name" | tr -d '"')
              arabic_name=$(echo "$arabic_name" | tr -d '"')
              price=$(echo "$price" | tr -d '"')
              old_price=$(echo "$old_price" | tr -d '"')
              last_update=$(echo "$last_update" | tr -d '"')
              
              # Calculate percentage change if old_price exists
              if [ -n "$trade_name" ] && [ -n "$price" ] && [ -n "$old_price" ] && [ "$old_price" != "" ]; then
                # Use awk for floating point calculation
                change_pct=$(awk -v new="$price" -v old="$old_price" 'BEGIN {
                  if (old != 0) {
                    change = ((new - old) / old) * 100
                    if (change >= 0) {
                      printf "+%.1f%%", change
                    } else {
                      printf "%.1f%%", change
                    }
                  } else {
                    print "N/A"
                  }
                }')
                
                # Determine arrow direction
                arrow=$(awk -v new="$price" -v old="$old_price" 'BEGIN {
                  if (new > old) print "📈"
                  else if (new < old) print "📉"
                  else print "➡️"
                }')
                
                echo "• *${arabic_name}* (${trade_name})" >> drugs_temp.txt
                echo "  ${old_price} ← ${price} ج.م ${arrow} (${change_pct})" >> drugs_temp.txt
              elif [ -n "$trade_name" ] && [ -n "$price" ]; then
                # Fallback if no old price
                echo "• *${arabic_name}* (${trade_name}) - ${price} ج.م" >> drugs_temp.txt
              fi
            done
            
            if [ -f drugs_temp.txt ]; then
              DRUGS_LIST=$(cat drugs_temp.txt | tr '\n' '|' | sed 's/|$//')
              echo "recent_drugs<<EOF" >> $GITHUB_OUTPUT
              cat drugs_temp.txt >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
              rm drugs_temp.txt
            else
              echo "recent_drugs=" >> $GITHUB_OUTPUT
            fi
          fi
      
      # إرسال إشعار بنجاح الـ Workflow مع معلومات الأدوية
      - name: Send Slack Notification - Success
        uses: rtCamp/action-slack-notify@v2
        if: success()
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_USERNAME: MediSwitch Bot
          SLACK_ICON_EMOJI: ':white_check_mark:'
          SLACK_COLOR: '#36a64f'
          SLACK_MESSAGE: |
            ✅ *تم تحديث قاعدة بيانات الأدوية بنجاح!*
            
            📅 *التاريخ والوقت:* $(date -u +"%Y-%m-%d")  |  ⏰ $(date -u +"%H:%M:%S UTC")
            
            📊 *الإحصائيات:*
            • إجمالي الأدوية: ${{ steps.summary.outputs.total_drugs }}
            • الوقت: $GITHUB_RUN_ID
            
            🆕 *آخر الأدوية المحدثة:*
            ${{ steps.summary.outputs.recent_drugs }}
            
            <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|عرض تفاصيل التنفيذ>

  # خطوة منفصلة لإرسال إشعار في حالة الفشل
  notify_failure:
    runs-on: ubuntu-latest
    needs: scrape-and-update
    if: failure()
    steps:
      - name: Send Slack Notification - Failure
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_USERNAME: MediSwitch Bot
          SLACK_ICON_EMOJI: ':x:'
          SLACK_COLOR: '#ff0000'
          SLACK_MESSAGE: |
            ❌ *فشل تحديث قاعدة بيانات الأدوية*
            
            *المستودع:* `${{ github.repository }}`
            *الوقت:* $(date -u +"%Y-%m-%d %H:%M:%S UTC")
            
            <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|عرض السجل لمعرفة السبب>
