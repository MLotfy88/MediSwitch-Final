# السياق التقني - MediSwitch (Tech Context)

## 💻 التقنيات المستخدمة

### الواجهة الأمامية (تطبيق الجوال)
-   **إطار العمل**: Flutter إصدار `3.29.2` (Stable).
-   **لغة البرمجة**: Dart إصدار `3.7.2`.
-   **المكتبات الأساسية**:
    -   `provider`: لإدارة حالة التطبيق.
    -   `sqflite`: للتعامل مع قاعدة البيانات المحلية SQLite (V16).
    -   `http`: لإجراء اتصالات الشبكة مع الـ Backend.
    -   `get_it`: لتطبيق نمط حقن التبعيات (Service Locator).
    -   `google_mobile_ads`: لعرض إعلانات AdMob.
    -   `cached_network_image`: لتخزين صور الأدوية مؤقتًا.
    -   `shared_preferences`: لتخزين الإعدادات البسيطة.

### الواجهة الخلفية (Backend - Cloudflare Serverless)
-   **المنصة**: `Cloudflare Workers` (JavaScript).
-   **قاعدة البيانات (Production)**: `Cloudflare D1` (Distributed SQL).
-   **قاعدة البيانات (Local/App)**: `SQLite` (تتزامن مع D1).
-   **استضافة لوحة التحكم**: `Cloudflare Pages`.
-   **الخدمات**:
    -   **المزامنة**: نقاط نهاية API مخصصة لسحب التحديثات (Delta Sync).
    -   **الأمان**: JWT للمصادقة وحماية البيانات.

---

## 🛠️ بيئة التطوير

### المتطلبات المسبقة
-   **نظام التشغيل**: Windows, Linux, أو macOS.
-   **IDE**: VS Code (موصى به) مع إضافات Flutter و SQLite.
-   **Node.js**: v18+ (لتشغيل Worker و Admin Dashboard).
-   **Flutter SDK**: أحدث إصدار ثابت.

### خطوات إعداد المشروع
1.  **نسخ المستودع**:
    ```bash
    git clone https://github.com/MLotfy88/MediSwitch-Final.git
    ```
2.  **تشغيل تطبيق Flutter**:
    ```bash
    cd lib
    flutter pub get
    flutter run
    ```
3.  **تشغيل لوحة التحكم (Admin)**:
    ```bash
    cd admin-dashboard
    npm install
    npm run dev
    ```
4.  **نشر الـ Backend (Worker)**:
    ```bash
    cd cloudflare-worker
    npm install
    npx wrangler deploy
    ```

---

## ⚠️ القيود التقنية

1.  **الأداء**:
    -   يجب أن يعمل التطبيق بسلاسة على أجهزة Android القديمة (API 28+).
    -   زمن استجابة البحث < 2 ثانية (وهو متحقق بفضل الفهرسة المحلية).

2.  **الشبكة (Offline-First)**:
    -   التطبيق يعمل بالكامل بدون إنترنت.
    -   الإنترنت مطلوب فقط للمزامنة والإعلانات.

3.  **حجم التطبيق**:
    -   يتم ضغط قاعدة البيانات (SQLite) وتقليل حجم الصور للحفاظ على ملف APK صغير.

4.  **الأمان**:
    -   جميع الاتصالات مشفرة (HTTPS).
    -   كلمات مرور المشرفين مشفرة (SHA-256/Salt).
