# السياق التقني - MediSwitch (Tech Context)

## 💻 التقنيات المستخدمة (Technologies)

### الواجهة الأمامية (تطبيق الجوال)
-   **إطار العمل**: Flutter إصدار `3.29.2` (Stable).
-   **لغة البرمجة**: Dart إصدار `3.7.2`.
-   **المكتبات الأساسية**:
    -   `provider: ^6.1.4`: لإدارة حالة التطبيق.
    -   `sqflite: ^2.3.3+1`: للتعامل مع قاعدة البيانات المحلية SQLite.
    -   `http: ^1.2.1`: لإجراء اتصالات الشبكة مع الـ Backend.
    -   `get_it: ^7.7.0`: لتطبيق نمط حقن التبعيات (Service Locator).
    -   `google_mobile_ads: ^5.1.0`: لعرض إعلانات AdMob.
    -   `cached_network_image: ^3.4.1`: لتخزين صور الأدوية مؤقتًا وتحسين الأداء.
    -   `shared_preferences: ^2.5.3`: لتخزين الإعدادات البسيطة (مثل الثيم واللغة).

### الواجهة الخلفية (Backend - Cloudflare Serverless)
-   **Platform**: `Cloudflare Workers` (JavaScript/Edge).
-   **Database**:
    -   **Cloudflare D1**: Distributed SQL Database (Production).
    -   **SQLite**: Local Development & Flutter App.
-   **Admin Hosting**: `Cloudflare Pages` (React SPA).
-   **Services**:
    -   **Notifications**: D1-based queue + Push API.
    -   **Analytics**: Custom D1 event tracking.
    -   **Monetization**: Server-side config & ad mediation control.

### البنية التحتية (Infrastructure)
-   **Host**: Cloudflare Global Network.
-   **CI/CD**: GitHub Actions -> Wrangler (Worker) / Pages (Dashboard).
-   **Tools**: `wrangler`, `nodejs`, `npm`.

## 🛠️ بيئة التطوير (Development Setup)

### المتطلبات المسبقة
-   **نظام التشغيل**: Windows, Linux, أو macOS.
-   **بيئة التطوير المتكاملة (IDE)**: VS Code (موصى به).
-   **Node.js**: v18+ (للتعامل مع Wrangler & React).
-   **Flutter SDK**: أحدث إصدار ثابت.

### steps إعداد المشروع
1.  **نسخ المستودع**:
    ```bash
    git clone https://github.com/MLotfy88/MediSwitch-Final.git
    ```
2.  **تشغيل تطبيق Flutter**:
    ```bash
    cd lib
    flutter pub get
    flutter run
    ```
3.  **تشغيل لوحة التحكم (Admin)**:
    ```bash
    cd admin-dashboard
    npm install
    npm run dev
    ```
4.  **نشر الـ Backend (Worker)**:
    ```bash
    cd cloudflare-worker
    npm install
    npx wrangler deploy
    ```

### أدوات التصحيح والاتصال
-   **ADB over Wi-Fi**: تم إعداده وتوثيقه في `README.md` لتسهيل تصحيح الأخطاء لاسلكيًا.
-   **SSH**: للوصول الآمن إلى الخادم البعيد (VPS) لإدارة النشر والصيانة.

## ⚠️ القيود التقنية (Constraints)

1.  **الأداء (Performance)**:
    -   يجب أن يعمل التطبيق بسلاسة تامة حتى على أجهزة Android القديمة (يدعم `minSdk 28` أي Android 9 وما فوق).
    -   زمن تحميل البيانات وعرض نتائج البحث يجب أن يكون لحظيًا (< 2 ثانية) لضمان تجربة مستخدم جيدة.

2.  **الشبكة (Network)**:
    -   التطبيق مصمم بمنهجية **Offline-First**. الاتصال بالإنترنت مطلوب فقط في حالات محددة: التحديث الأول للبيانات، المزامنة الدورية، وعرض الإعلانات. يجب أن تعمل جميع الوظائف الجوهرية بدون إنترنت.

3.  **حجم التطبيق (App Size)**:
    -   يجب الحفاظ على حجم ملف التثبيت (APK) صغيرًا قدر الإمكان لتسهيل التحميل في المناطق ذات الإنترنت الضعيف.

4.  **الأمان (Security)**:
    -   يجب حماية مفاتيح API الخاصة بخدمات الطرف الثالث (مثل AdMob).
    -   يجب تأمين الاتصال بين التطبيق والـ Backend باستخدام HTTPS (جاري العمل عليه).

## 🔗 الاعتماديات الخارجية (External Dependencies)
-   **Google AdMob**: المصدر الرئيسي لتحقيق الدخل في النسخة المجانية.
-   **Google Fonts**: استخدام خط "Noto Sans Arabic" لضمان قراءة مريحة للنصوص العربية.
-   **Lucide Icons**: مكتبة أيقونات عصرية ومتسقة لواجهة المستخدم.
